package com.videostreaming.api.model;

/**
 * Enum Class that represents the status of a payment transaction
 */
public enum PaymentStatus {
    SUCCESS,  // Payment processed successfully
    FAILED,   // Payment failed
    PENDING   // Payment is pending (not used in this assignment, but good for future)
}
